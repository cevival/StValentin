import { defineConfig } from "astro/config";

export default defineConfig({
  // GitHub Pages configuration
  site: "https://cevival.github.io",
  base: "/valentine-astro",
  // ou si c'est un repo user/org:
  // base: '/',

  integrations: [],
});
